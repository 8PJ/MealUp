import Container from "react-bootstrap/esm/Container";

function NotFound(props) {
    return (
        <Container className="d-flex justify-content-center notFoundContainer">
            Page not Found
        </Container>
    );
}

export default NotFound;
